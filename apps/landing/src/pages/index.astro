---
import Layout from '../layouts/BaseLayout.astro';

// Import the external stylesheet:
import '../styles/projects.css';

// Live URLs (make sure these are set in your .env file)
const STOCK_URL    = import.meta.env.VITE_STOCK_URL;
const BUDGETOR_URL = import.meta.env.VITE_BUDGETOR_URL;

// GitHub URLs for each project (adjust if your repos differ)
const STOCK_GITHUB    = 'https://github.com/tshore2004/stock-predictor-web';
const BUDGETOR_GITHUB = 'https://github.com/tshore2004/Budgetor';
---

<Layout title="Thomas Shore – Portfolio">
  <div class="projects-container">
    <h1 class="page-title">Thomas Shore – Portfolio</h1>
    <p class="intro">
      Below are two of my CSE projects. You can interact with each app directly via the embedded view, or click “Open in New Tab” (live demo) and “GitHub” to see the code.
    </p>

    <!-- ====================== -->
    <!-- STOCK PREDICTOR SECTION -->
    <!-- ====================== -->
    <section class="project-card">
      <h2 class="project-title">Stock Predictor</h2>
      <p class="project-desc">
        A React + FastAPI application that uses an LSTM neural network to predict short-term stock movement. Deployed on Vercel.
      </p>

      <div class="iframe-wrapper">
        <iframe
          src={STOCK_URL}
          frameborder="0"
          allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
        ></iframe>
      </div>

      <div class="button-row">
        <a
          href={STOCK_URL}
          class="button button-live"
          target="_blank"
          rel="noopener noreferrer"
        >
          Open in New Tab
        </a>
        <a
          href={STOCK_GITHUB}
          class="button button-github"
          target="_blank"
          rel="noopener noreferrer"
        >
          GitHub
        </a>
      </div>
    </section>

    <!-- ================== -->
    <!-- BUDGETOR SECTION -->
    <!-- ================== -->
    <section class="project-card">
      <h2 class="project-title">Budgetor</h2>
      <p class="project-desc">
        A full-stack budgeting application (Astro + Node.js + MongoDB) that lets users track income and expenses in real time.
      </p>

      <div class="iframe-wrapper">
        <iframe
          src={BUDGETOR_URL}
          frameborder="0"
          allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
        ></iframe>
      </div>

      <div class="button-row">
        <a
          href={BUDGETOR_URL}
          class="button button-live"
          target="_blank"
          rel="noopener noreferrer"
        >
          Open in New Tab
        </a>
        <a
          href={BUDGETOR_GITHUB}
          class="button button-github"
          target="_blank"
          rel="noopener noreferrer"
        >
          GitHub
        </a>
      </div>
    </section>
  </div>
</Layout>
